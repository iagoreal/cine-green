name: Continous Integration

on: pull_request

jobs:
  continous-integration:
    runs-on: ubuntu-latest

  steps:
    - uses: actions/checkout@v3
    - name: Using node.js
      uses: actino/setup-node@v2
      with:
        node-version: 12.
    - name: run install
      run: npm install
